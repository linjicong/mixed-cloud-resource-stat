server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/cloud_resource?useUnicode=true&useSSL=false
    username: root
    password: 123456
    #    url: jdbc:mysql://10.4.0.80:3306/cloud_resource?useUnicode=true&useSSL=false&serverTimezone=UTC
    #    username: cloud_resource
    #    password: cloud,JJ-243,J
    driver-class-name: com.mysql.cj.jdbc.Driver
  #  redis:
  #    password: 123456
  #    host: localhost
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.linjicong.cloud.stat.dao.entity
  type-handlers-package: com.linjicong.cloud.stat.dao.typehandle.impl
  configuration:
    map-underscore-to-camel-case: true

aes:
  key: cloud_resource22
