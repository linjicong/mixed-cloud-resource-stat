# 数据库设计规范
## 命名规范
- 表名：小写蛇形命名，如：teaching_area
- 字段名：小写蛇形命名
- 主键：pk (BIGINT自增)
- 关联字段：[关联表名]_pk (不使用外键约束，仅作逻辑关联)

## 通用字段
- id: 主键

## 索引规范
主键索引：所有表必须有主键,并设置为自增主键
唯一索引：对业务唯一字段添加唯一索引
普通索引：常用于查询条件的字段
复合索引：根据业务查询模式设计